import re
from src.advanced_forensics import detect_encryption_status, detect_hidden_text_patterns, detect_attachments, detect_3d_and_multimedia, detect_pdf_a_compliance

# Basic test script to see the functions we plan to modify
print("Ready")
